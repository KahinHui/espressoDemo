<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kahin.espressodemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EspressoDemo">
        <activity
            android:name=".ui.main.activity.compose.StaggeredGridActivity"
            android:exported="true"
            android:label="@string/title_activity_staggered_grid"
            android:theme="@style/Theme.EspressoDemo.NoActionBar" />
        <activity
            android:name=".ui.main.activity.compose.ComposeActivity"
            android:exported="true"
            android:label="@string/title_activity_compose"
            android:theme="@style/Theme.EspressoDemo.NoActionBar" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.activity.MainActivity" />
        <activity android:name=".ui.main.fragment.FragmentActivity" />
        <activity android:name=".ui.main.activity.CallActivity" />
        <activity android:name=".ui.main.activity.WebViewActivity" />
    </application>

</manifest>